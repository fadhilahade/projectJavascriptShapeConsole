const rectangleWidth = 50
const rectangleLength = 20

const rectangleArea = rectangleWidth * rectangleLength

console.log(`${rectangleArea} = ${rectangleWidth} * ${rectangleLength}`)

const triangleWidth = 82
const triangleLength = 15

const triangleArea = (1 / 2) * triangleWidth * triangleLength

console.log(`${triangleArea} = 1/2 *  ${triangleWidth} * ${triangleLength}`)